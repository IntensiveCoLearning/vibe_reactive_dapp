---
timezone: UTC+8
---

# fox896

**GitHub ID:** fox896

**Telegram:**

## Self-introduction

Let’s vibe Reactive dApp

## Notes

<!-- Content_START -->
# 2026-03-17
<!-- DAILY_CHECKIN_2026-03-17_START -->
今天做了一下任务2，账户上没有gas在本地跑了一下哈哈

![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/vibe_reactive_dapp/main/assets/fox896/images/2026-03-17-1773737344445-image.png)
<!-- DAILY_CHECKIN_2026-03-17_END -->

# 2026-03-16
<!-- DAILY_CHECKIN_2026-03-16_START -->

7.1 实现基本响应式函数

响应式合约（RC）专为Uniswap V2平台设计，该合约旨在根据预定义的条件执行止损单。

合同

[UniswapDemoStopOrderReactive](https://github.com/Reactive-Network/reactive-smart-contract-demos/blob/main/src/demos/uniswap-v2-stop-order/UniswapDemoStopOrderReactive.sol)合约旨在监控 Uniswap V2 上的流动性池事件，具体来说，就是跟踪这些Sync事件以确定何时满足止损单的条件。当这些条件被触发时，它会在以太坊区块链上执行回调交易来执行止损单。

关键组成部分

7.1.2 事件声明

事件声明：诸如Subscribed，VM，AboveThreshold, CallbackSent，Done等等的事件用于记录和跟踪区块链上的合约操作。

// 事件声明：像 Subscribed, VM, AboveThreshold, CallbackSent, Done 这样的事件用于在区块链上记录和追踪合约的操作。 // SPDX许可证标识：GPL-2.0 或更高版本 pragma solidity >=0.8.0; // 声明使用的Solidity编译器版本 // 引入Reactive框架的基础库 import '../../../lib/reactive-lib/src/interfaces/IReactive.sol'; import '../../../lib/reactive-lib/src/abstract-base/AbstractReactive.sol'; // 定义一个结构体，用来表示Uniswap交易对中的两种代币储备量 struct Reserves { uint112 reserve0; // 第一种代币的储备量 uint112 reserve1; // 第二种代币的储备量 } // 合约名：Uniswap演示停止订单响应合约 contract UniswapDemoStopOrderReactive is IReactive, AbstractReactive { // 事件：已订阅 // 当成功订阅一个监听目标后触发，记录订阅的详细信息。 event Subscribed( address indexed service\_address, // 服务地址 address indexed _contract, // 被监听的合约地址 uint256 indexed topic_0 // 监听的事件主题 ); event VM(); // 事件：虚拟机。可能表示代码正在ReactVM中开始执行。 // 事件：超过阈值 // 这是\*\*核心事件\*\*，当监听到的价格满足触发条件时发出，包含了所有相关数据。 event AboveThreshold( uint112 indexed reserve0, // 索引后的储备量0，便于查询 uint112 indexed reserve1, // 索引后的储备量1 uint256 coefficient, // 计算出的价格系数 uint256 threshold // 预设的触发阈值 ); event CallbackSent(); // 事件：回调已发送。表示向目标链发送操作的请求已发出。 event Done(); // 事件：完成。表示整个流程已处理完毕。

想象一下你设置了一个股价报警器。这些事件就是报警器发出的不同提示音：

-   Subscribed： “嘀”一声，表示报警器（合约）已经成功锁定了你要监控的那支股票（Uniswap交易对）。
    

-   VM： “嗡”一声，表示报警器内部的芯片（ReactVM）开始启动计算了。
    

-   AboveThreshold： “铃铃铃！！！” 这是最重要的警报声！表示股价（价格系数）已经达到了你设定的目标位（阈值），并把当前股价信息报了出来。
    

-   CallbackSent： “指令已下达”提示音。表示报警器已经向你的股票交易软件（目标链）发出了“立刻卖出”的指令。
    

-   Done： “哔”一声，表示整个报警-下单流程处理完毕。
    

7.1.3 合约变量

UNISWAP\_V2\_SYNC\_TOPIC\_0 和 STOP\_ORDER\_STOP\_TOPIC\_0 是常量，分别代表Uniswap的Sync事件和本合约Stop事件的主题标识。

// CALLBACK\_GAS\_LIMIT 是为回调交易设置的Gas上限。

// 像 triggered, done, pair, stop\_order, client, token0, coefficient, threshold 这些变量存储了停止订单的状态和配置。

// ---------- 【固定设置区】这些是写死的规则，不会改变 ---------- uint256 private constant SEPOLIA\_CHAIN\_ID = 11155111; // 目标链：Sepolia测试网的ID uint256 private constant UNISWAP\_V2\_SYNC\_TOPIC\_0 = 0x1c411e9a...; // 要监听的事件：Uniswap V2的“Sync”（资金池同步）事件 uint256 private constant STOP\_ORDER\_STOP\_TOPIC\_0 = 0x9996f0dd...; // 要触发的事件：停止订单的“Stop”事件 uint64 private constant CALLBACK\_GAS\_LIMIT = 1000000; // 执行回调时可使用的最大燃料费 // ---------- 【工作记忆区】这些是ReactVM实例中合约的专属状态，会随着操作变化 ---------- // （回忆：这是存储在ReactVM“车间”里的记忆） bool private triggered; // 是否已被触发（true表示价格已达标） bool private done; // 是否已完成全部处理 address private pair; // 要监控的Uniswap交易对地址 address private stop\_order; // 目标链上等待被触发的停止订单合约地址 address private client; // 下这个订单的客户地址 bool private token0; // 布尔值，表示我们是监控token0的价格（true）还是token1的价格（false） uint256 private coefficient; // 计算出的实时价格系数 uint256 private threshold; // 用户设定的触发价格阈值

把合约想象成你设定的那个自动交易机器人：

固定设置（常量）：是机器人出厂时你给它写好的永久指令手册。

SEPOLIA\_CHAIN\_ID： 手册第1条：“去Sepolia测试网干活。”

UNISWAP\_V2\_SYNC\_TOPIC\_0： 手册第2条：“你的耳朵要专门听Uniswap资金池‘资金变化’的这种特定声音。”

CALLBACK\_GAS\_LIMIT： 手册第3条：“每次下单，油费最多不能超过100万单位。”

工作记忆（状态变量）：是机器人工作时手里的临时记事本，记录了当前这笔生意的所有细节。（关键：这个记事本只放在ReactVM车间里！）

pair= “ETH/USDT交易池”；threshold= “价格到 3500 就行动”

triggered= “当前价格是3499，还没到”（false）。

当监听到价格变成3501时，它立刻在记事本上把 triggered改成 “true”，并算出 coefficient= 3501，然后大喊一声 AboveThreshold!事件。

整体流程结合你的架构知识：

在 Reactive Network（控制塔）：合约实例用 UNISWAP\_V2\_SYNC\_TOPIC\_0等常量，向控制塔注册：“请帮我监听XX地址的这个事件。”成功后，发出 Subscribed事件。

当事件发生时：控制塔将事件数据打包，发送给对应的 ReactVM（专属车间）。车间启动，发出 VM()事件。

在 ReactVM 内：车间里的合约实例（使用pair, threshold等状态变量）计算当前价格。如果满足条件，就更新triggered，并发出包含详细数据的 AboveThreshold事件。接着，它请求控制塔向目标链（Sepolia）的stop\_order地址发送一个交易（回调），并发CallbackSent事件。最后标记done为true，发出Done事件。

这个合约完美展示了之前讲的“一个合约，两个实例，不同状态”的架构：控制塔的实例负责“感知”，车间的实例负责“计算和决策”。

7.1.4 合约逻辑

构造函数

构造函数：初始化合约，存储关键信息，并设置“监听”（订阅事件）

constructor( address _pair, // 要监控的Uniswap交易对地址（如ETH/USDT池） address_ stop\_order, // 目标链上待触发的停止订单合约地址 address _client, // 客户地址（你） bool_ token0, // 监控的是token0（ETH）的价格？true代表监控ETH相对USDT的价格 uint256 _coefficient, // 计算系数（用于价格计算，常为10的幂次方，如1e18） uint256_ threshold // 触发阈值（如3500，表示ETH价格<=3500USDT时触发） ) payable { // 初始化机器人的“工作记忆” triggered = false; // 还没触发 done = false; // 还没完成 pair = _pair; stop_order = _stop_order; client = _client; token0 =_ token0; coefficient = _coefficient; threshold =_ threshold; // 关键：只有不在“车间”（ReactVM）里运行时，才去设置“耳朵” if (!vm) { // 如果当前不在ReactVM中（即在Reactive Network控制塔中） // 设置第一个耳朵：监听Uniswap交易对的“资金变化”事件 service.subscribe(SEPOLIA\_CHAIN\_ID, pair, UNISWAP\_V2\_SYNC\_TOPIC\_0, ...); // 设置第二个耳朵：监听停止订单合约的“停止”事件（用于确认订单完成） service.subscribe(SEPOLIA\_CHAIN\_ID, stop\_order, STOP\_ORDER\_STOP\_TOPIC\_0, ...); } }

通俗解释：

当你创建这个机器人（部署合约）时，你告诉它：

“去盯着这个交易池（\_pair），如果价格跌到这个数（\_threshold），就触发那个订单合约（\_stop\_order），订单主人是我（\_client）。”

然后，机器人在控制塔（!vm为真）里安装了两个“耳朵”：

监听价格变化：专门听Uniswap池子的资金变动（SYNC事件）。

监听订单完成：专门听停止订单合约的完成信号（STOP事件）。

重要：这个“安装耳朵”的步骤只在控制塔里做一次。之后，每当“耳朵”听到声音，控制塔就会把消息送到专属车间（ReactVM）去处理。

2\. react() 函数 - 车间的“大脑”和决策中心

这是整个机器人的核心逻辑，在ReactVM车间里运行。每当控制塔送来一个事件消息，这个函数就被调用。

function react(LogRecord calldata log) external vmOnly { // 只能在ReactVM车间里运行 assert(!done); // 确保任务还没完成 // 情况一：如果消息来自“停止订单合约”（说明是订单完成的通知） if (log.\_contract == stop\_order) { // 验证：1. 我们之前已经触发过订单了(triggered==true)； // 2. 事件主题匹配；3. 事件参数里的交易对和客户都对得上。 if (triggered && log.topic\_0 == STOP\_ORDER\_STOP\_TOPIC\_0 ...) { done = true; // 标记整个流程完成 emit Done(); // 发出“完成”的提示音 } } // 情况二：如果消息来自其他地方（预期就是Uniswap的价格变化事件） else { // 解码事件数据，得到最新的两种代币储备量 Reserves memory sync = abi.decode([log.data](http://log.data), ( Reserves )); // 核心判断：如果价格跌到阈值以下，并且是第一次触发 if (below\_threshold(sync) && !triggered) { emit CallbackSent(); // 提示音：开始发送指令 // 准备发给目标链的“操作指令单” bytes memory payload = abi.encodeWithSignature( "stop(address,address,address,bool,uint256,uint256)", ... // 参数：交易对、客户、监控哪种代币、系数、阈值 ); triggered = true; // 标记为“已触发”，防止重复触发 // 发出最重要的指令：让控制塔去目标链执行操作！ emit Callback(log.chain\_id, stop\_order, CALLBACK\_GAS\_LIMIT, payload); } } }

**通俗解释**：

这个函数就像机器人的大脑，在车间里处理两种消息：

1.  “订单已完成”消息：如果机器人听到停止订单合约说“你那个订单我处理完了”，它就检查这消息是不是给自己的（核对交易对和客户）。如果是，并且自己之前确实触发过订单（triggered==true），它就大功告成，发出Done提示音，然后休息。
    
2.  “价格有变动”消息：这是主要工作。机器人拿到最新的储备量，立刻用below\_threshold函数（后面解释）算一下当前价格。 **如果价格还没到阈值**：什么都不做，继续等。 **如果价格达标了**：它立刻做三件事： a. 大喊一声CallbackSent!（我要下指令了！）。 b. 写一张详细的“操作指令单”（payload），上面写明要在目标链的哪个合约、调用哪个函数、传递什么参数。 c. 把自己的triggered状态改成true（防止手抖重复下单）。 d. 把这张指令单交给**控制塔**（通过emit Callback事件），并说：“按这个单子，立刻去Sepolia链上执行！”
    

**3\. below\_threshold() 函数 - 价格计算器**

这是一个纯计算函数，判断当前价格是否跌破阈值。

function below\_threshold(Reserves memory sync) internal view returns (bool) { if (token0) { // 如果监控的是token0（比如ETH）的价格 // 计算： (USDT储备量 _系数) / ETH储备量 <= 阈值？ // 这相当于：ETH价格（以USDT计） = (USDT数量 / ETH数量)_ 系数 <= 阈值？ return (sync.reserve1 _coefficient) / sync.reserve0 <= threshold; } else { // 如果监控的是token1（比如USDT）的价格 return (sync.reserve0_ coefficient) / sync.reserve1 <= threshold; } }

举例：

假设 token0 = true（监控ETH价格），coefficient = 1e18，threshold = 3500 \* 1e18。

当前储备：reserve0 (ETH) = 100，reserve1 (USDT) = 400,000。

计算价格：(400,000 _1e18) / 100 = 4,000_ 1e18。

判断：4000 <= 3500？不成立，所以不触发。

如果USDT储备降到350,000，价格算出来是3500，3500 <= 3500成立，触发！

执行流程总结（结合架构）

部署与订阅（在Reactive Network）：

你部署合约，构造函数在控制塔实例中运行，设置了两个事件监听器（“耳朵”）。

监听与转发（Reactive Network的工作）：

控制塔的“耳朵”一直听着。一旦Uniswap发生Sync事件，控制塔就把这个事件日志打包，送到对应的ReactVM车间。

计算与决策（在ReactVM）：

车间的react()函数被调用。它用below\_threshold()计算价格，发现达标了。

车间生成一个“回调指令单”（payload），并发出一个Callback事件（这是一个特殊的、给控制塔的信号）。

执行与确认（回到Reactive Network，再到目标链）：

控制塔听到车间发出的Callback事件，就按照指令单，真的去Sepolia测试网调用那个stop\_order合约的stop函数，完成链上交易。

这个stop函数执行成功后，会在Sepolia链上发出一个Stop事件。

控制塔的另一个“耳朵”听到了这个Stop事件，又把它送到车间。

车间的react()函数再次被调用，这次匹配到是订单完成的消息，于是标记done = true，流程结束。

结论

这个合约完美展示了响应式架构的威力：

Reactive Network（控制塔）：负责跨链监听和最终执行。它不关心价格怎么算，只负责传递事件和发送交易。

ReactVM（车间）：负责核心业务逻辑（计算价格、判断条件）。它安全、隔离，专心做计算。

两个实例，各司其职：一个合约，两份实例。控制塔的实例负责“感知”，车间的实例负责“思考”。它们通过事件（Callback）协同工作，实现了自动化的、跨链的、条件触发的去中心化交易。
<!-- DAILY_CHECKIN_2026-03-16_END -->

# 2026-03-14
<!-- DAILY_CHECKIN_2026-03-14_START -->


# **Uniswap 的工作原理**

Uniswap V2 是一个去中心化金融协议，运行在以太坊区块链上，支持去中心化代币的自动化交易。其核心是流动性池和智能合约，实现了无缝的代币兑换。理解类似 Uniswap 的去中心化交易所 (DEX) 对于理解 DeFi、智能合约应用和响应式应用场景至关重要。本课程结束时，您将掌握以下知识：

-   Uniswap V2 资金池的结构和功能，包括它们如何促进代币交换和流动性提供。
    
-   Uniswap V2 中的定价机制由常数乘积公式 (x \* y = k) 控制。
    
-   交换和同步事件在维护池动态和提供透明度方面的执行和意义。
    
-   通过代码示例对 Uniswap V2 智能合约中的交换功能进行实际理解。
    

## **Uniswap V2**

Uniswap V2 中的流动性池本质上是两种代币的储备，形成一个交易对。这些流动性池是 Uniswap 生态系统的基石，使用户无需传统做市商即可交易代币。

在 Uniswap V2 中，每笔交易或流动性提供都是通过以太坊区块链上的交易执行的。这些交易是公开的，可以[在 Etherscan](https://etherscan.io/tx/0x7b969e8a74ae9891e322311ca5fe6e5d7bcb53ac3412b4189d84683961043503)或类似的区块浏览器上查看。

Uniswap V2 中的智能合约管理流动性池，规定代币交换规则，并确保交易按照协议的算法执行，该算法通常被称为恒定乘积做市商模型。

### **常数乘积**

Uniswap V2 智能合约使用以下公式：x \* y = k，其中 x 和 y 分别代表流动性池中两种代币的数量，k 为常数。该公式在维持流动性池总流动性的同时，允许代币价格根据交易活动波动。

代码示例：以下是 Uniswap V2 swap() 函数的一个简化代码片段（请参阅代码下方的说明）：

```
function swap(uint amount0Out, uint amount1Out, address to, bytes calldata data) external {
require(amount0Out > 0 || amount1Out > 0, "UniswapV2: INSUFFICIENT_OUTPUT_AMOUNT");
(uint112 reserve0, uint112 reserve1,) = getReserves(); // fetches reserves of the pool
require(amount0Out < reserve0 && amount1Out < reserve1, "UniswapV2: INSUFFICIENT_LIQUIDITY");

    uint balance0;
    uint balance1;
    {
        uint amount0In = reserve0 - (balance0 = reserve0 - amount0Out);
        uint amount1In = reserve1 - (balance1 = reserve1 - amount1Out);
        require(amount0In > 0 || amount1In > 0, "UniswapV2: INSUFFICIENT_INPUT_AMOUNT");

        uint balanceAdjusted0 = balance0 * 1000 - amount0In * 3;
        uint balanceAdjusted1 = balance1 * 1000 - amount1In * 3;
        require(balanceAdjusted0 * balanceAdjusted1 >= uint(reserve0) * uint(reserve1) * (1000**2), "UniswapV2: K");

        // Emit the Swap event
        emit Swap(msg.sender, amount0In, amount1In, amount0Out, amount1Out, to);
    }

    _update(balance0, balance1, reserve0, reserve1);

    if (amount0Out > 0) _safeTransfer(token0, to, amount0Out);
    if (amount1Out > 0) _safeTransfer(token1, to, amount1Out);

    if (data.length > 0) {
        IUniswapV2Callee(to).uniswapV2Call(msg.sender, amount0Out, amount1Out, data);
    }
}
```

在此函数中：

-   `amount0Out`和`amount1Out`分别是调用者希望从池中接收的每个代币的数量。
    
-   该函数首先检查输出金额是否为正数，以及互换交易是否会耗尽资金池的储备。
    
-   然后，它将输入金额（`amount0In`和`amount1In`）计算为初始储备金与互换后新余额之间的差额。
    
-   `balanceAdjusted0`该合约确保在扣除 0.3% 的费用（和`balanceAdjusted1`计算）后，交易保持恒定的产品不变性 (k) 。
    
-   调用该`_update`函数以使用新的余额更新资金池的储备金。
    
-   代币被转移到接收者的地址`to`。
    
-   如果有回调数据（`data`），它会调用`uniswapV2Call`接收地址上的函数，这可用于更复杂的交互，例如闪存交换。
    
-   该`Swap`事件在计算完输入和输出数量之后、更新储备金之前立即触发。事件`Swap`会记录发送者、流入和流出资金池的代币数量以及代币接收者。
    

这种逻辑概括了 Uniswap V2 中兑换交易的本质，即平衡资金池的储备以维持恒定的产品，同时促进代币兑换。

我们将主要关注`Swap`用于监控区块链活动并基于此运行响应式合约的事件。由于资金池智能合约的代码不会改变，因此每次交易的不同信息大部分都会记录在事件中。接下来，让我们更详细地讨论一下我们最感兴趣的两种事件类型：`Swap`和`Sync`。

## **Uniswap**

该`Swap`事件在 Uniswap V2 池中每次发生交易时都会发出。它提供有关交易的重要信息，例如交易中涉及的代币数量以及交易者和接收者的地址。

事件结构示例：

```
event Swap(
address indexed sender,
uint amount0In,
uint amount1In,
uint amount0Out,
uint amount1Out,
address indexed to
);
```

在此事件中：

-   `sender`这是发起交换的地址。
    
-   `amount0In`和`amount1In`分别是发送到资金池的相应代币的数量。
    
-   `amount0Out`和`amount1Out`分别是从资金池中发送的相应代币的数量。
    
-   `to`是接收输出令牌的地址。
    

您可以在Etherscan 上该交易的[事件列表](https://etherscan.io/tx/0x7b969e8a74ae9891e322311ca5fe6e5d7bcb53ac3412b4189d84683961043503#eventlog)中看到此事件。

`Sync`当 Uniswap V2 资金池的储备金更新时，就会触发此事件。此事件会在流动性增加或减少后，或资金池发生直接代币转入或转出时触发。该`Sync`事件有助于跟踪资金池储备金的当前状态。

事件结构示例：

```
event Sync(uint112 reserve0, uint112 reserve1);
```

Uniswap V2 的基本原理，Uniswap V2 是 DeFi 的基石，它通过流动性池和智能合约实现自动化交易。主要内容包括：

-   **Uniswap V2 资金池：**这些资金池由两种代币组成，无需传统做市商即可实现无缝交易和流动性供应。每笔交易都由恒定乘积做市商模型管理，该模型负责维持资金池的流动性平衡。
    
-   **常数乘积公式：**公式（x \* y = k）确保两种代币数量的乘积保持不变，从而可以根据交易活动进行动态定价。
    
-   **兑换和同步事件：**该`Swap`事件提供有关交易的详细信息，包括代币数量和地址，同时该`Sync`事件还会跟踪储备金的更新。这些事件对于监控 Uniswap 活动并将其与响应式合约集成至关重要。
    
-   **代码机制：**提供的代码示例说明了 Uniswap V2 中该函数的核心功能`swap`，展示了合约如何维持流动性并确保准确的代币交换。
<!-- DAILY_CHECKIN_2026-03-14_END -->

# 2026-03-13
<!-- DAILY_CHECKIN_2026-03-13_START -->



5.1 Oracles 是如何运作的

5.1.1 概述

reactive 合约擅长监控链上事件并执行相应的链上操作。然而，在智能合约生态系统中，存在一个专门用于将链下数据导入区块链的特殊类别，即预言机。在响应式合约可以响应的众多事件中，预言机发出的事件尤为重要。本文将深入探讨预言机的概念，为接下来我们将要探索的用例奠定更清晰的基础。通过剖析区块链框架中预言机的机制和影响，我们旨在帮助您掌握充分理解响应式合约在与现实世界数据交互方面的潜力和实用性所需的知识。

5.1.2 编预言家

在区块链和智能合约领域，与现实世界交互的必要性带来了一项独特的挑战。智能合约运行于确定性环境中，其中每个操作都必须可验证且可重复。然而，为了充分发挥智能合约的潜力，通常需要访问来自外部世界的数据——无论是价格信息、天气预报还是其他链下信息。这一需求引出了预言机问题：如何在不牺牲去中心化和无需信任的核心原则的前提下，将链下数据获取到区块链上。

5.1.3 解决Oracle问题

预言机问题通过被称为预言机的实体来解决，预言机充当区块链（链上）和外部世界（链下）之间的桥梁。预言机从众多外部来源获取数据并将其输入区块链。这些数据可能来自金融市场的API（用于获取价格信息）、政府数据库（用于获取公共记录）或物联网设备（用于获取现实世界的物理数据）。预言机效用的核心在于其能够以最小化信任的方式验证这些数据并将其传递给智能合约。

谁来签署预言机向区块链输入数据的交易，这个问题引出了确保数据完整性和可信度的机制。通常情况下，交易使用预言机服务提供商的私钥进行签名。

为了增强安全性并降低故障或恶意篡改的风险，许多去中心化预言机网络采用多重签名协议。多重签名要求一组参与者中预先设定数量的签名才能授权交易，从而确保任何单一实体都无法单方面向区块链提交数据。这种方法为流程增加了一层去中心化和安全保障，符合区块链系统的无需信任特性。

Chainlink 和 Band Protocol 是一些常用的预言机提供商。这些平台聚合来自多个数据源的数据，确保数据完整性并降低数据被篡改的风险。

5.1.4 实际应用与实例

预言机为智能合约解锁了众多应用场景，使其能够对现实世界的事件和数据做出反应。一些值得注意的应用包括：

-   DeFi 平台：利用价格预言机管理借贷利率、清算和资产互换。
    
-   保险：根据可信预言机报告的可验证事件（如自然灾害）触发赔付。
    
-   在线博彩：智能合约为无需信任的在线博彩提供了强大的技术解决方案，而预言机则将体育赛事结果的数据提供给此类系统。
<!-- DAILY_CHECKIN_2026-03-13_END -->

# 2026-03-12
<!-- DAILY_CHECKIN_2026-03-12_START -->




4.1 订阅机制如何运作

本节课，我们将深入探讨订阅，这是响应式网络的一个关键特性，它允许响应式网络自动响应其他合约发出的事件。当这些事件发生时，订阅合约可以自动执行预定义的逻辑。

4.1.1 如何实现订阅

subscribe在响应式合约中，订阅是通过响应式网络系统合约中的方法设置的。通常，这是在构造函数中初始化订阅的过程，不过订阅也可以动态管理。我们将在本文末尾讨论动态订阅。

响应式合约还必须处理由于在响应式网络（具有系统合约）和其部署者的私有 ReactVM（其中不存在系统合约）上部署而导致的回滚。

4.1.2 订阅服务接口

ISubscriptionService接口用作响应式合约的事件订阅服务，响应式合约可以使用此服务根据某些条件订阅特定事件，并在这些事件发生时接收通知。

pragma solidity >=0.8.0;

import './IPayable.sol';

interface ISubscriptionService is IPayable {

function subscribe(

uint256 chain\_id,

address \_contract,

uint256 topic\_0,

uint256 topic\_1,

uint256 topic\_2,

uint256 topic\_3

) external;

function unsubscribe(

uint256 chain\_id,

address \_contract,

uint256 topic\_0,

uint256 topic\_1,

uint256 topic\_2,

uint256 topic\_3

) external;

}

这两个函数的参数互为镜像：

chain\_id：uint256代表EIP155事件的源链 ID。

\_contract：发出该事件的源链合约的地址。

topic\_0，，，topic\_1：活动的主题，即topic\_2价值观。topic\_3uint256

由于需要查找和删除订阅，取消订阅是一项成本较高的操作。允许重复或重叠的订阅，但客户端必须确保幂等性。

4.1.3 iReactive Interferce

IReactive接口定义了响应式合约的标准，这些合约可以接收和处理与其订阅匹配的事件通知。它扩展了IPayer接口，表明它包含了与支付相关的功能。

pragma solidity >=0.8.0;

import './IPayer.sol';

interface IReactive is IPayer {

struct LogRecord {

uint256 chain\_id;

address \_contract;

uint256 topic\_0;

uint256 topic\_1;

uint256 topic\_2;

uint256 topic\_3;

bytes data;

uint256 block\_number;

uint256 op\_code;

uint256 block\_hash;

uint256 tx\_hash;

uint256 log\_index;

}

event Callback(

uint256 indexed chain\_id,

address indexed \_contract,

uint64 indexed gas\_limit,

bytes payload

);

function react(LogRecord calldata log) external;

}

LogRecord 结构：一种数据结构，表示事件的详细日志，包括：

chain\_id：原始区块链的 ID。

\_contract：事件发生的合同地址。

topic\_0至topic\_3：事件日志的索引主题。

data：其他未索引的事件数据。

block\_number：记录事件时的区块编号。

op\_code：事件分类的操作代码。

block\_hash：包含该事件的区块的哈希值。

tx\_hash：触发​​该事件的交易哈希值。

log\_index：事务中日志的索引。

回调事件：用于指示响应式合约已被触发的事件。它包括：

chain\_id：原始区块链的 ID。

\_contract：发出事件的合约地址。

gas\_limit：为回调函数分配的最大 gas 量。

payload回调期间发送的数据有效载荷。

react 函数：处理事件通知的主要入口点。

log（类型LogRecord）：包含事件详情。

4.1.4构造器订阅

以下是如何在构造函数中进行订阅的方法：

// State specific to reactive network instance of the contract

address private \_callback;

// State specific to ReactVM instance of the contract

uint256 public counter;

constructor(

address \_service,

address \_contract,

uint256 topic\_0,

address callback

) payable {

service = ISystemContract(payable(\_service));

if (!vm) {

service.subscribe(

CHAIN\_ID,

\_contract,

topic\_0,

REACTIVE\_IGNORE,

REACTIVE\_IGNORE,

REACTIVE\_IGNORE

);

}

\_callback = callback;

}

4.1.5 订阅标准

在配置响应式合约中的订阅时，应遵守以下规则：

通配符用法：address(0)用于指示按任意合约地址筛选，uint256(0)指示按任意链 ID 筛选，以及REACTIVE\_IGNORE用于主题筛选，指示按任意主题筛选。

具体价值：至少要有一个标准是具体的价值，以确保有意义的订阅。

订阅特定

以下是如何订阅特定合约下所有事件的方法0x7E0987E5b3a30e3f2828572Bb659A548460a3003：

service.subscribe(CHAIN\_ID, 0x7E0987E5b3a30e3f2828572Bb659A548460a3003, REACTIVE\_IGNORE, REACTIVE\_IGNORE, REACTIVE\_IGNORE, REACTIVE\_IGNORE)

订阅特定事件主题（Uniswap V2 同步

另一种方法是订阅所有 Uniswap V2 同步事件topic\_0 0x1c411e9a96e071241c2f21f7726b17ae89e3cab4c78be50e062b03a9fffbbad1：

service.subscribe(CHAIN\_ID, 0, 0x1c411e9a96e071241c2f21f7726b17ae89e3cab4c78be50e062b03a9fffbbad1, REACTIVE\_IGNORE, REACTIVE\_IGNORE, REACTIVE\_IGNORE)

组合

您可以组合这些参数来订阅特定合约的事件，方法0x7E0987E5b3a30e3f2828572Bb659A548460a3003如下topic\_0 0x1c411e9a96e071241c2f21f7726b17ae89e3cab4c78be50e062b03a9fffbbad1：

service.subscribe(CHAIN\_ID, 0x7E0987E5b3a30e3f2828572Bb659A548460a3003, 0x1c411e9a96e071241c2f21f7726b17ae89e3cab4c78be50e062b03a9fffbbad1, REACTIVE\_IGNORE, REACTIVE\_IGNORE, REACTIVE\_IGNORE)

处理来自不同

要响应来自不同来源的多个事件，可以subscribe在构造函数中使用多次调用：

constructor(

address \_service,

address \_contract1,

address \_contract2,

uint256 topic\_0,

address callback

) payable {

// Initialize the subscription service

SubscriptionService service = SubscriptionService(payable(\_service));

if (!vm) {

// First subscription

service.subscribe(

CHAIN\_ID,

\_contract1,

REACTIVE\_IGNORE,

REACTIVE\_IGNORE,

REACTIVE\_IGNORE,

REACTIVE\_IGNORE

);

// Second subscription

service.subscribe(

CHAIN\_ID,

address(0),

topic\_0,

REACTIVE\_IGNORE,

REACTIVE\_IGNORE,

REACTIVE\_IGNORE

);

// Add more subscriptions here as needed

}

// Assign the callback

\_callback = callback;

}
<!-- DAILY_CHECKIN_2026-03-12_END -->

# 2026-03-11
<!-- DAILY_CHECKIN_2026-03-11_START -->





3.1 ReactVM 和 Reactive network 作为双状态环境

RC合约的另一个关键特性：它们在Reactive network和ReactVM中以两个具有独立状态的实例存在。理解这一概念对于RC合约至关重要。

3.1.1 ReactVM 和 Reactive network之间的区别

每个RC合约都有两个实例，一个位于Reactive network，另一个在ReactVM 中运行。是一个为了**高性能**而设计的特殊系统架构。核心思想是：**把一份智能合约拆成两个“分身”，让它们分工合作，就像工厂的流水线一样。**

为什么要把一个合约做成两个实例？

这完全是为了速度和效率。

分工明确，互不干扰：总控系统（Reactive Network）专心处理事件调度和消息传递；工作站（ReactVM）专心执行计算。两者并行工作，不会因为一方忙碌而卡住另一方。

应对海量事件：当短时间内有大量事件（比如交易、数据更新）涌来时，这个架构就像一条高效的流水线。总控系统快速分拣事件，同时唤醒多个对应的工作站同时开工，从而极大地提升了整体处理能力。

简单来说：

Reactive Network 是协调者，负责调度和通信；ReactVM 是执行者，负责运行合约代码。把一份合约同时放在这两个地方运行，是一种通过“并行化”来提升系统性能的聪明设计。

Reactive Network 的运行方式与典型的 EVM 区块链类似，但它增加了系统合约，允许用户订阅和取消订阅以太坊、BNB、Polygon 或 Optimism 上的源链事件。每个部署地址都拥有一个专用的 ReactVM。

ReactVM 是一个受限虚拟机，旨在隔离地处理事件。从一个地址部署的合约在同一个 ReactVM 中执行。它们可以相互交互，但不能与响应式网络上的其他合约交互。

ReactVM 中的合约可以通过两种方式与外部世界交互，这两种方式都通过Reative network实现：

-   它们会对订阅的特定事件做出反应，并在这些事件发生时执行。
    
-   ReactVM 根据事件输入执行代码，向Reative network发送请求，以获取回调到目标链，从而执行相应的链上操作。
<!-- DAILY_CHECKIN_2026-03-11_END -->

# 2026-03-10
<!-- DAILY_CHECKIN_2026-03-10_START -->






2.1 事件与回调的工作原理

在以太坊中，事件使智能合约能够通过在满足特定条件时记录特定信息来与外部世界通信。这使得去中心化应用程序（dApp）无需持续轮询区块链即可触发和响应事件。事件由以太坊虚拟机（EVM）索引，使其易于搜索，这对于监控区块链活动（例如转账、合约更新和预言机提供的价格变动）尤其有用。

2.1.1 EVM事件

事件是一种**廉价的、不可篡改的、用于对外广播“发生了什么”的日志**。它不负责“做”，只负责“说”，但“说”出来的话可以被无数“耳朵”听到并做出反应。

当智能合约发出事件时，事件数据会被记录在日志中，永久上链。发事件不会直接改变合约的变量状态。但它提供了一种基于事件参数记录和检索信息的方法。

开发者用even关键字定义事件名称，以及记录数据的模版。触发事件时，智能合约用关键字emit接事件名称和要记录的数据，发出记录次事件。

pragma solidity ^0.8.0;

contract DestinationContract {

//定义合约

//用关键字event定义事件

event CallbackReceived(uint256 indexed id, string message);

//回调处理函数

function handleCallback(uint256 id, string memory message)

external {

//用关键字emit发出此事件

emit CallbackReceived(id, message);

// 这里可以添加额外的逻辑来处理回调。

}

}

当事件被发出后，外部应用程序（前端网页，后端服务，RC）可以监听到此事件。程序监听到后，可以自动触发后序操作。（通过指定事件签名以及（可选的）过滤参数，这些应用程序可以在事件发生时订阅实时更新。）这种机制对于创建响应迅速且交互性强的区块链应用程序至关重要。

2.1.2 RC合约中的事件处理

RC合约必须实现IReactive接口来处理传入的事件。

pragma solidity >=0.8.0; import ‘./IPayer.sol’; //IReactive接口继承了 IPayer的功能（通过 is IPayer）。 //IPayer接口通常定义了支付Gas费或处理资金的相关函数， //是响应式合约能“自主支付”的基础。 interface IReactive is IPayer { //用于封装从区块链上捕获到的一个完整事件日志。 //当响应式合约订阅的某个事件发生时，Reactive Network 会将这些详细信息打包成一个 LogRecord实例，传递给合约处理。 struct LogRecord { uint256 chain\_id; address \_contract; uint256 topic\_0; uint256 topic\_1; uint256 topic\_2; uint256 topic\_3; bytes data; uint256 block\_number; uint256 op\_code; uint256 block\_hash; uint256 tx\_hash; uint256 log\_index; } //发起跨链回调的标准事件 event Callback( uint256 indexed chain\_id, address indexed \_contract, uint64 indexed gas\_limit, bytes payload ); function react(LogRecord calldata log) external; }

日志记录结构：一种结构化数据类型，LogRecord用于包含有关事件日志的详细信息：

-   chain\_id：事件发生的区块链 ID。
    
-   \_contract：发出该事件的合约的地址。
    
-   topic\_0至topic\_3：日志的索引主题。
    
-   data：事件日志中未建立索引的数据。
    
-   block\_number：事件发生的街区编号。
    
-   op\_code：可能表示操作码。
    
-   block\_hash、tx\_hash和log\_index：用于追踪事件来源和上下文的其他标识符。
    

回调事件：用于通知订阅者特定事件发生的事件：

-   chain\_id：事件的区块链 ID。
    
-   \_contract：合约发行地址。
    
-   gas\_limit：回调函数分配的最大 gas 值。
    
-   payload：随回调一起返回的编码数据。
    

react 函数：处理传入事件通知的关键函数。

-   接受一个LogRecord输入，使响应式合约能够动态处理事件日志。
    
-   标记为external，仅允许从合约外部调用。
    

RC network持续监控事件的日志，并将合约中定义的条件进行匹配，符合条件时，网络触发相应的react（）方法，并且传递相关详细信息。

RC合约可以访问所有标准的EVM功能。但是，它们运行在私有的ReactVM环境中，这限制了它们只能与同一部署者部署的合约进行交互。这种隔离机制确保响应式合约在处理来自响应式网络的事件时，能够维持一个受控且安全的环境。

2.1.3回调到目标链

RC合约可以t通过特定的格式发出日志记录，在目标链发起交易。这些记录会被响应式网络接收，然后响应式网络会在相关的链上执行所需的交易。

整个过程可以被概括为一个三步的自动化管道：

\[RC 合约\] – 1.发出 Callback 事件 --> \[响应式网络\] – 2.中继与处理 --> \[目标链\] – 3.执行交易 --> \[目标合约\]

发出回调事件

要请求在目标链上执行操作，用户必须Callback在响应式合约中触发一个事件。触发后，智能合约会发出此事件，并提供响应式网络创建和提交交易所需的关键信息。

event Callback( uint256 indexed chain\_id, address indexed \_contract, uint64 indexed gas\_limit, bytes payload );

-   chain\_id：目标网络的 EIP155 链 ID。
    
-   \_contract：合同目的地地址。
    
-   gas\_limit：目标链上交易的 gas 限制。
    
-   payload：用于指定目标函数调用的编码数据。此数据指导响应式网络如何对目标合约执行预期操作。
    

处理回调

当Callback事件触发时，响应式网络会检测到它并处理该事件，该事件会将交易详情编码为特定格式。然后，响应式网络使用提供的和 payload将交易提交到目标链上指定的合约。chain\_idgas\_limit

目标链上的合约执行该交易，完成RC所期望的操作（例如，在Uniswap上执行兑换、转移资产、更新状态等）。

**构造 payload**:

-   payload是指导“在目标合约上具体调用哪个函数、传入什么参数”的编码数据。
    
-   使用 abi.encodeWithSignature等标准Solidity工具进行编码，格式与普通交易调用数据完全相同。
    
-   关键安全机制：出于安全目的，响应式网络**会自动**将 payload中编码的第一个参数（前160位）替换为**调用此RC的ReactVM的地址**。这意味着，在RC编码时，第一个参数位置应预留（如设为 address(0)），它将在网络层被替换为真实的、经过验证的发送者身份。
    

// 文档中的编码示例 bytes memory payload = abi.encodeWithSignature( “stop(address,address,address,bool,uint256,uint256)”, address(0), // 此位置将被自动替换为 ReactVM 地址 pair, // 第二个及之后的参数是您自定义的业务参数 client, token0, coefficient, threshold );
<!-- DAILY_CHECKIN_2026-03-10_END -->

# 2026-03-09
<!-- DAILY_CHECKIN_2026-03-09_START -->








1.1 Reactive Network 概述

Reactive Network 是一个与 EVM 兼容的链，它围绕 Reactive Contracts (RC) 构建——RC 是一种事件驱动的智能合约，用于跨链和链上自动化。

响应式合约监控EVM链上的事件日志，并在满足条件时自动执行Solidity逻辑。响应式合约无需等待用户或机器人触发交易，而是持续运行并决定何时发送跨链回调交易——本质上**为智能合约提供了“如果……那么……”的自动化功能**。

这使得构建条件性跨链工作流成为可能，例如：

-   自动止损和止盈订单
    
-   清算保护
    
-   自动投资组合再平衡
    
-   产量优化
    
-   跨链工作流程
    

1.2 响应式合同是什么？

1.2.1 响应式

-   响应式合约（RC）代表着我们与区块链技术交互方式的一次范式转变。与由用户交易运行的传统智能合约不同，响应式合约会主动监控与 EVM 兼容的链上的事件并做出响应。它们根据已实现的逻辑处理这些事件，并在区块链上自主执行后续操作。这项创新引入了一种去中心化的机制，无需人工干预即可自动响应链上事件。
    

1.2.2 为什么需要响应式

在以太坊世界中，智能合约彻底改变了我们执行无需信任协议的方式。传统上，这些合约只有在用户发起交易后才会生效。这存在固有的局限性。首先，智能合约无法在没有外部指令（无论是来自用户还是像交易机器人这样的自动化脚本）的情况下自主发起操作或响应区块链事件。这就需要持有私钥并引入中心化的控制点。

响应式合约（RC）的出现正是为了解决这一限制。RC旨在自主响应以太坊虚拟机（EVM）中的事件，并触发区块链生态系统中的后续操作。这种能力使得复杂的逻辑得以实现，它可以从多个链获取信息，并在无需中央监管的情况下跨平台执行更改或交易

1.2.3 响应式合约的优势

-   去中心化：RC在区块链上独立运行，消除了中心化的控制点，并通过降低操纵或故障的风险来提高安全性。
    
-   自动化：RC 会根据链上事件自动执行智能合约逻辑，实现高效的实时响应
    
-   跨链互操作：RC可以与多个区块链进行交互
    
-   增强效率和功能：RC 提高了智能合约的效率，支持复杂金融工具、动态 NFT 和创新 DeFi 应用等高级功能。
    
-   DeFi 及其他领域的创新： RC 为 DeFi 和其他区块链应用带来了新的可能性，例如自动化交易和动态治理，从而创建了一个更具响应性和互联性的区块链生态系统。
    

1.3 响应式合约总结

RC代表了区块链技术的一项重大飞跃，无需用户干预，可以自主执行逻辑。与传统的EVM不同，RC可以自动响应来自多个区块链的事件，从而实现灵活的跨链交互。

反应式合约（RCs）与传统EVM智能合约的核心区别在于**执行范式从“被动响应”转变为“主动监听”**。具体对比如下：

| 对比维度 | 传统EVM智能合约 | 反应式合约 | | 触发方式​ | 被动：必须由外部账户（EOA）或合约账户主动发送一笔交易来调用。 | 主动：持续监听链上事件（如代币转账、状态变更），条件满足时自动触发执行。 | | 执行模式​ | 请求-响应：像一台自动售货机，有人投币（发送交易）才工作。 | 事件驱动：像一个7x24小时在线的自动化机器人，监控环境并自动行动。 | | 持续性​ | 无状态（在交易执行期间）：仅在处理交易时运行，执行结束后即“休眠”。 | 有状态（在事件周期内）：作为后台进程持续运行，等待特定事件。 | | Gas支付方​ | 由发起交易的用户账户支付。 | 通常由RCs的部署者或维护者预付Gas，或通过类似“Gas Station Network”的机制处理。 | | 主要设计目标​ | 实现确定性的业务逻辑和状态变更。 | 实现跨合约、跨区块甚至跨链的复杂自动化工作流。 | | 典型应用​ | 代币发行、去中心化交易所、简单众筹。 | 自动清算、限价订单、跨链桥接、DAO自动化治理、游戏事件响应。 |

**简单来说：**

-   传统合约是**工具**，需要你手动使用。
    
-   反应式合约是**助理**，在你设定的规则下替你自动完成工作。
    

这种区别使得RCs能构建出更复杂、更智能、更自动化的去中心化应用，但同时也带来了更高的设计复杂度和Gas成本管理挑战。
<!-- DAILY_CHECKIN_2026-03-09_END -->
<!-- Content_END -->
